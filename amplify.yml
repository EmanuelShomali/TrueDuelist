version: 1

env:
  variables:
    key: value

backend:
  phases:
    preBuild:
      commands:
        #- *enter command*
    build:
      commands:
        #- *enter command*
    postBuild:
        commands:
        #- *enter command*

frontend:
  phases:
    preBuild:
      commands:
        - cd aws_react
        - npm ci
    build:
      commands:
        - npm run-script build
  artifacts:
    baseDirectory: build
    files:
        - '**/*'
    discard-paths: yes

  cache:
    paths:
        - node_modules/**/*