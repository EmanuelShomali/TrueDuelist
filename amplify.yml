version: 1

backend:
  phases:
    preBuild:
      commands:
        #- *enter command*
    build:
      commands:
        #- *enter command*
    postBuild:
        commands:
        #- *enter command*

frontend:
  phases:
    preBuild:
      commands:
        - cd aws_react
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
        - '**/*'
  cache:
    paths:
        - node_modules/**/*
        