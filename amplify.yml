version: 1

env:
  variables:
    key: value

backend:
  phases:
    preBuild:
      commands:
        #- *enter command*
    build:
      commands:
        #- *enter command*
    postBuild:
        commands:
        #- *enter command*

frontend:
  phases:
    preBuild:
      commands:
        - cd aws_react
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /
    files:
        - '**/*'
    discard-paths: yes
    
  cache:
    paths:
        - path
        - path

test:
  phases:
    preTest:
      commands:
        #- *enter command*
    test:
      commands:
        #- *enter command*
    postTest:
      commands:
        #- *enter command*
  artifacts:
    files:
        - location
        - location
    configFilePath: *location*
    baseDirectory: *location*